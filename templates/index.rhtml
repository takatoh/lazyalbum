<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01 Transitional//EN"
                      "http://www.w3.org/TR/html4/loose.dtd">

<html lang="ja">

<head>
  <meta http-equiv="Content-Type" content="text/html; charset=Shift_JIS">
  <title>Index - <%= page_title %></title>
  <% if stylesheet %><link rel="stylesheet" type="text/css" href="<%= stylesheet %>"><% end %>
</head>

<body>

  <div class="header"><h1><%= page_title %></h1></div>

  <div class="pageheader"><a href="<%= cgi_url %>">Index</a></div>

  <div class="pagebody">
    <table align="center">
      <tr class="tableheader"><th width="50">No.</th><th width="500">Title</th></tr>
      <% items.each_with_index do |itm, idx| %>
      <tr class="<%= odd_or_even(idx + 1) %>"><td class="center"><%= idx + 1 %></td>
          <td><a href="<%= url_to_entry(itm[:entry]) %>"><%= itm[:title] %></a></td></tr>
      <% end %>
    </table>
  </div>

</body>

</html>
